{
  "name": "@healthtrack/api",
  "version": "1.0.0",
  "description": "HealthTrack Lambda Functions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "deploy": "npm run build && cd ../infra && npm run deploy"
  },
  "dependencies": {
    "@healthtrack/schemas": "*",
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/client-textract": "^3.450.0",
    "@aws-sdk/client-comprehendmedical": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "@aws-sdk/s3-request-presigner": "^3.450.0",
    "aws-lambda": "^1.0.7",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.2.0"
  }
}
